[project]
name = "algorithms-project-69"
version = "0.1.0"
description = "hexlet algorithms final task 1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.9",
]
